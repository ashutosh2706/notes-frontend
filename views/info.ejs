<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>401</title>
    <style>
        body {
            background-color: #f8f9fa;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
        }

        .dismiss-button {
            width: 40%;
            font-size: 16px;
            font-weight: 600;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            padding: 10px;
            margin: 10px 0;
            border: 3px solid #000;
            border-radius: 30px;
            background-color: transparent;
            color: #000;
            cursor: pointer;
        }

        .dismiss-button:hover {
            background-color: #000000;
            color: #fff;
            transition: 0.3s;
        }

        .container {
            text-align: center;
        }

        .icon {
            font-size: 5em;
            color: #dc3545;
            margin-bottom: 20px;
        }

        img {
            width: 1.3em;
            height: 1.3em;
        }

        h1 {
            color: #dc3545;
            font-size: 2em;
        }

        p {
            color: #495057;
            font-size: 1.3em;
            font-weight: 600;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="icon">
            <img src="error.svg" alt="Error">
        </div>
        <h1>
            <%= title%>
        </h1>
        <p>
            <%= message%>
        </p>
        <button class="dismiss-button" onclick="redirect()">OK</button>
    </div>
    <script>
        function redirect() {
            window.location.href = '/login';
        }
    </script>
</body>

</html>